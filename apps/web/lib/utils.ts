// Re-export utilities from shared package
export {
  cn,
  formatPrice,
  slugify,
  truncate,
  calculateDiscount,
  debounce,
  isClient,
  generateId,
  formatDate,
  delay,
} from '@bijoux/utils';
